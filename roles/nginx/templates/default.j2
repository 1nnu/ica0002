server {
    listen 80 default_server;
    server_name _;
	
    location / {
        uwsgi_pass {{ uwsgi_socket }};
        include uwsgi_params;
    }

    location /prometheus {
        {% for vm in groups['prometheus'] %}
        proxy_pass http://{{ vm }}:9090;
        {% endfor %}
    }
}